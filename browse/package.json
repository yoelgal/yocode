{
  "name": "@yocode/browse",
  "version": "0.1.0",
  "description": "Headless browser daemon for yocode",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile dist/browse",
    "dev": "bun run src/cli.ts"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
